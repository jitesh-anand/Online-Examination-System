create database OTS
use OTS
create table register
(
id int identity(1,2),
fname varchar(30),
lname varchar(30),
age integer,
sex varchar(10),
dob date,
qual varchar(20),
email varchar(30) primary key,
pass varchar(20)
);
select * from register

select * from qtabel1
select * from qtabel2


